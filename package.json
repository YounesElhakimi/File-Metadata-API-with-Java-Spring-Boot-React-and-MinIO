{
  "name": "file-metadata-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd frontend && npm start",
    "install-all": "npm install && cd frontend && npm install",
    "dev": "cd frontend && npm start",
    "build": "cd frontend && npm run build"
  },
  "dependencies": {
    "concurrently": "^7.6.0"
  }
}